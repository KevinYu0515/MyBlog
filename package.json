{
  "name": "blogcustom",
  "version": "1.0.0",
  "description": "Here is YuKai Blog",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "algolia": "atomic-algolia",
    "dev": "tinacms dev -c \"hugo server -D\"",
    "build": "tinacms build && hugo"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tinacms/cli": "^1.5.14",
    "atomic-algolia": "^0.3.19",
    "tinacms": "^1.5.6"
  }
}
